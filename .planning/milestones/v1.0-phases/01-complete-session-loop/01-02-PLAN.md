---
phase: 01-complete-session-loop
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/hooks/useAudioManager.ts
  - app/components/TrackControl.tsx
  - app/components/RoomMixControls.tsx
  - app/routes/_index.tsx
  - app/routes/relax.tsx
autonomous: true
requirements:
  - AUDIO-01
  - AUDIO-02
must_haves:
  truths:
    - "When an audio track fails, its TrackControl row shows a red error icon badge and retry button"
    - "Clicking retry re-loads the audio source and attempts playback; on success the error badge and retry button disappear"
    - "Toggling a failed track pause off/on clears the error state (per D-10)"
    - "On relax page load, previously paused tracks remain paused (restored from localStorage)"
  artifacts:
    - path: "app/hooks/useAudioManager.ts"
      provides: "trackErrors state, retryTrack function, initialPausedTracks parameter"
      contains: "trackErrors"
    - path: "app/components/TrackControl.tsx"
      provides: "hasError prop, onRetry prop, error badge icon, retry button"
      contains: "hasError"
    - path: "app/components/RoomMixControls.tsx"
      provides: "trackErrors prop, onTrackRetry prop threaded to TrackControl"
      contains: "trackErrors"
    - path: "app/routes/relax.tsx"
      provides: "Restored pausedTracks on load via initialPausedTracks parameter"
      contains: "initialPausedTracks"
  key_links:
    - from: "app/hooks/useAudioManager.ts"
      to: "HTMLAudioElement.play()"
      pattern: "catch.*trackErrors"
    - from: "app/hooks/useAudioManager.ts"
      to: "HTMLAudioElement.onerror"
      pattern: "onerror"
    - from: "app/routes/_index.tsx"
      to: "app/components/RoomMixControls.tsx"
      pattern: "trackErrors"
    - from: "app/routes/relax.tsx"
      to: "app/hooks/useAudioManager.ts"
      pattern: "initialPausedTracks"
---

<objective>
Surface audio playback errors inline per track and restore paused track state on the relax page.

Purpose: Audio tracks currently fail silently (caught in empty catch blocks). Users don't know when a track isnt playing. This plan surfaces per-track failures with inline error badge (D-08) and retry button (D-09), clears errors on retry success or pause toggle (D-10). It also fixes the relax page to restore pausedTracks from localStorage (D-11).

Output:
- app/hooks/useAudioManager.ts -- Extended with trackErrors, retryTrack, initialPausedTracks param, setAllPausedTracks
- app/components/TrackControl.tsx -- Error badge and retry button
- app/components/RoomMixControls.tsx -- Error state and retry handler props
- app/routes/_index.tsx -- Error state and retry passed to RoomMixControls
- app/routes/relax.tsx -- Restored pausedTracks from localStorage
</objective>

<execution_context>
@/Users/hongphuc/playground/vite-virtual-cafe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hongphuc/playground/vite-virtual-cafe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-complete-session-loop/01-CONTEXT.md
@.planning/phases/01-complete-session-loop/01-RESEARCH.md
@.planning/phases/01-complete-session-loop/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Extend useAudioManager with trackErrors, retryTrack, initialPausedTracks, setAllPausedTracks</name>
<files>app/hooks/useAudioManager.ts</files>

<read_first>
  app/hooks/useAudioManager.ts
  app/constants/audioConfig.ts
  app/types/audio.ts
</read_first>

<action>
Read app/hooks/useAudioManager.ts, then apply targeted modifications (do NOT rewrite from scratch):

1. Add initialPausedTracks? as second function parameter: `export function useAudioManager(tracks: MixerTrack[], initialPausedTracks?: Record)`

2. Update pausedTracks useState: `initialPausedTracks ?? Object.fromEntries(tracks.map((t) => [t.label, false]))`

3. Add trackErrors state after pausedTracks: `const [trackErrors, setTrackErrors] = useState<Record>({});`

4. In initializeAudio forEach, after audioElement.volume = 0; add:
```
audioElement.onerror = () => {
  setTrackErrors((prev) => ({ ...prev, [track.label]: true }));
};
```

5. Replace try/catch around Promise.all with per-track error handling:
```
await Promise.all(
  DEFAULT_TRACKS.filter((track) => !pausedTracks[track.label]).map(
    async (track) => {
      const el = audioElementsRef.current.get(track.label);
      if (!el) return;
      try {
        const p = el.play();
        if (p) await p;
        setTrackErrors((prev) => ({ ...prev, [track.label]: false }));
      } catch {
        setTrackErrors((prev) => ({ ...prev, [track.label]: true }));
      }
    }
  )
);
```

6. In sync volumes useEffect, change empty catch to: `setTrackErrors((prev) => ({ ...prev, [track.label]: true }));`

7. In toggleTrackPause, after the `if (nextPaused)` block, add: `setTrackErrors((prev) => ({ ...prev, [label]: false }));`

8. Add retryTrack after toggleTrackPause:
```
const retryTrack = async (label: string) => {
  const el = audioElementsRef.current.get(label);
  if (!el) return;
  try {
    el.load();
    const p = el.play();
    if (p) await p;
    setTrackErrors((prev) => ({ ...prev, [label]: false }));
  } catch { /* error remains */ }
};
```

9. Add setAllPausedTracks wrapper: `const setAllPausedTracks = (paused: Record) => { setPausedTracks(paused); };`

10. Update return to include: trackErrors, retryTrack, setAllPausedTracks.
</action>

<acceptance_criteria>
- useAudioManager has initialPausedTracks? optional second param
- pausedTracks init uses initialPausedTracks ?? Object.fromEntries(...)
- trackErrors state initialized as {}
- audioElement.onerror assigned in initializeAudio forEach
- Per-track play() in initializeAudio sets trackErrors on failure, clears on success
- Sync effect .catch() sets trackErrors
- toggleTrackPause clears error on unpause
- retryTrack calls load() then play(), clears on success
- setAllPausedTracks wraps setPausedTracks
- Return includes trackErrors, retryTrack, setAllPausedTracks
- npm run typecheck passes with zero errors
</acceptance_criteria>

<verify>
  <automated>grep -c "initialPausedTracks" app/hooks/useAudioManager.ts</automated>
  <automated>grep -c "trackErrors" app/hooks/useAudioManager.ts</automated>
  <automated>grep -c "retryTrack" app/hooks/useAudioManager.ts</automated>
  <automated>grep -c "setAllPausedTracks" app/hooks/useAudioManager.ts</automated>
  <automated>npm run typecheck 2>&1 | tail -5</automated>
</verify>

<done>useAudioManager extended with trackErrors, retryTrack, initialPausedTracks, setAllPausedTracks. Errors captured on play() rejection, onerror events, cleared on retry success or pause toggle. TypeScript passes.</done>
</task>

<task type="auto">
<name>Task 2: Update TrackControl and RoomMixControls for error display and retry</name>
<files>
  app/components/TrackControl.tsx
  app/components/RoomMixControls.tsx
</files>

<read_first>
  app/components/TrackControl.tsx
  app/components/RoomMixControls.tsx
</read_first>

<action>
Modify TrackControl.tsx:

1. Add to TrackControlProps: `hasError: boolean; onRetry: () => void;`
2. Destructure hasError, onRetry in function params
3. Wrap icon span in `<div className="relative">` for absolute-positioned error badge
4. After icon span inside the wrapper, conditionally render error badge when hasError:
```
{hasError && (
  <span className="absolute -right-1 -top-1 material-symbols-outlined text-[14px]" style={{ color: "#ba1a1a" }} title="Track failed to load. Tap to retry.">error</span>
)}
```
5. Conditionally render retry button alongside pause button when hasError:
```
{hasError && (
  <button type="button" onClick={onRetry} aria-label={`Retry ${label}`} className="flex-shrink-0 rounded-lg p-1.5 transition-colors hover:bg-surface-container-high" title="Track failed to load. Tap to retry.">
    <span className="material-symbols-outlined text-[18px] text-outline transition-colors hover:text-primary">refresh</span>
  </button>
)}
```
6. Wrap retry + pause buttons in `<div className="flex items-center gap-1">`

Modify RoomMixControls.tsx:

1. Add to RoomMixControlsProps: `trackErrors: Record; onTrackRetry: (label: string) => void;`
2. Destructure trackErrors, onTrackRetry
3. In tracks.map, pass: `hasError={trackErrors[track.label] || false}` and `onRetry={() => onTrackRetry(track.label)}`

UI-SPEC colors: error badge #ba1a1a (error), retry default #877366 (outline/), retry hover #8f4a00 (primary). Title attr per UI-SPEC copywriting.
</action>

<acceptance_criteria>
- TrackControlProps has hasError: boolean and onRetry: () => void
- TrackControl renders error icon (material "error") when hasError=true, with absolute -right-1 -top-1 positioning, color #ba1a1a
- TrackControl renders retry button (material "refresh") when hasError=true, with text-outline/hover:text-primary
- Error badge and retry button have title="Track failed to load. Tap to retry."
- RoomMixControlsProps has trackErrors: Record and onTrackRetry: (label) => void
- TrackControl in RoomMixControls receives hasError and onRetry props
- npm run typecheck passes
</acceptance_criteria>

<verify>
  <automated>grep -c "hasError" app/components/TrackControl.tsx</automated>
  <automated>grep -c "onRetry" app/components/TrackControl.tsx</automated>
  <automated>grep -c "trackErrors" app/components/RoomMixControls.tsx</automated>
  <automated>grep -c "onTrackRetry" app/components/RoomMixControls.tsx</automated>
  <automated>npm run typecheck 2>&1 | tail -5</automated>
</verify>

<done>
TrackControl renders error badge and retry button when hasError is true. RoomMixControls threads trackErrors and onTrackRetry down to each TrackControl. TypeScript passes.
</done>
</task>

<task type="auto">
<name>Task 3: Thread error props in _index.tsx and restore pausedTracks in relax.tsx</name>
<files>
  app/routes/_index.tsx
  app/routes/relax.tsx
</files>

<read_first>
  app/routes/_index.tsx
  app/routes/relax.tsx
  app/constants/audioConfig.ts (for STORAGE_KEY)
</read_first>

<action>
A) Modify app/routes/_index.tsx:

In the RoomMixControls component usage, add two new props:
```
trackErrors={audio.trackErrors}
onTrackRetry={(label: string) => audio.retryTrack(label)}
```

The RoomMixControls section currently looks like:
```
<RoomMixControls
  soundEnabled={audio.soundEnabled}
  tracks={tracks}
  pausedTracks={audio.pausedTracks}
  onToggleSound={audio.toggleSound}
  onTrackVolumeChange={(label, value) => ...}
  onTrackPauseToggle={(label) => audio.toggleTrackPause(label)}
/>
```

Add the two new props after the existing ones, before the closing />.

B) Modify app/routes/relax.tsx:

1. Add `useRef` to the React import: `import { useEffect, useRef, useState } from "react";`

2. Read pausedTracks from localStorage synchronously before the useAudioManager call (guarded for SSR with typeof window check):
```
let initialPausedTracks: Record<string, boolean> | undefined;
if (typeof window !== "undefined") {
  try {
    const saved = window.localStorage.getItem(STORAGE_KEY);
    if (saved) {
      const parsed = JSON.parse(saved) as SavedPreferences;
      if (parsed.pausedTracks) {
        initialPausedTracks = parsed.pausedTracks;
      }
    }
  } catch { /* ignore */ }
}
```
Place this BEFORE the component function body's state declarations (at the top of the function, before useState calls).

3. Change `const audio = useAudioManager(tracks);` to pass initial paused state:
```
const audio = useAudioManager(tracks, initialPausedTracks);
```

4. Add a loadedRef flag to prevent the save effect from overwriting on initial mount (Pitfall 4 mitigation):
```
const loadedRef = useRef(false);
```

5. In the load useEffect, set `loadedRef.current = true;` at the end (after all state restoration is complete).

6. In the save useEffect, add a guard at the top:
```
if (!loadedRef.current) return;
```

This prevents the save effect from writing default "all unpaused" state over the just-restored localStorage data on initial mount.
</action>

<acceptance_criteria>
- _index.tsx RoomMixControls receives trackErrors={audio.trackErrors} and onTrackRetry prop
- relax.tsx imports useRef from React
- relax.tsx reads pausedTracks from localStorage synchronously (typeof window guard) before useAudioManager call
- relax.tsx passes initialPausedTracks to useAudioManager as second argument
- relax.tsx has loadedRef useRef(false) guard
- Load useEffect sets loadedRef.current = true after restoration
- Save useEffect guards with if (!loadedRef.current) return;
- npm run typecheck passes with zero errors
</acceptance_criteria>

<verify>
  <automated>grep -c "trackErrors" app/routes/_index.tsx</automated>
  <automated>grep -c "onTrackRetry" app/routes/_index.tsx</automated>
  <automated>grep -c "initialPausedTracks" app/routes/relax.tsx</automated>
  <automated>grep -c "loadedRef" app/routes/relax.tsx</automated>
  <automated>npm run typecheck 2>&1 | tail -5</automated>
</verify>

<done>
_index.tsx threads trackErrors and retryTrack to RoomMixControls. relax.tsx reads pausedTracks from localStorage synchronously, passes as initialPausedTracks to useAudioManager, and guards the save effect with loadedRef to prevent overwrite. TypeScript passes.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage (pausedTracks restore) | read on page load, same-origin only |
| HTMLAudioElement | Remote MP3s from imissmycafe.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | D (DoS) | HTMLAudioElement remote MP3 load failure | mitigate | Error badge + retry button per D-08/D-09; non-blocking per-track errors |
| T-02-02 | T (Tampering) | localStorage pausedTracks | accept | Paused state is ephemeral UI preference; no security impact if tampered |
| T-02-03 | S (Spoofing) | Retry button click handler | accept | Retry is a convenience operation; no security impact |
</threat_model>

<verification>
1. npm run typecheck passes with zero errors
2. npm run lint passes (or pre-existing issues only)
3. npm run build completes without errors
4. Manual: start audio, wait for a track to fail (or simulate by disconnecting), verify red error badge appears
5. Manual: click retry button, verify error badge clears on successful retry
6. Manual: pause a failed track, unpause it, verify error badge clears (D-10)
7. Manual: on focus page, pause some tracks. Navigate to /relax, verify those tracks are still paused
8. Manual: verify that tracks NOT paused on focus page are unpaused on relax page
</verification>

<success_criteria>
- [ ] Inline red error icon badge on TrackControl row per D-08 (material "error", #ba1a1a, absolute positioned)
- [ ] Retry button alongside error badge per D-09 (material "refresh", default #877366, hover #8f4a00)
- [ ] Error flagged on both initial load failures (onerror) and mid-playback failures (play() rejection) per D-09
- [ ] Error clears on successful retry (retryTrack resolves) per D-10
- [ ] Error clears on track pause toggle (unpause) per D-10
- [ ] On relax page load, pausedTracks restored from localStorage matching _index.tsx behavior per D-11
- [ ] Use of loadedRef guard prevents save effect from overwriting restored data on initial mount (Pitfall 4 mitigation)
- [ ] Input validation (retryTrack guards against missing audio element)
- [ ] Functional state updates for error state avoid stale closures (Pitfall 3 mitigation)
- [ ] Error state does not leak between tracks (per-track map, not shared boolean)
</success_criteria>

<output>
After completion, create `.planning/phases/01-complete-session-loop/01-02-SUMMARY.md`
</output>
