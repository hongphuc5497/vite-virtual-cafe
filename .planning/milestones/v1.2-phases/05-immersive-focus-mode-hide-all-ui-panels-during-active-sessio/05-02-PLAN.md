---
phase: 5
phase_name: Immersive Focus Mode
plan: 05-02
plan_name: Integrate immersive mode into _index.jsx
wave: 2
depends_on: ["05-01"]
autonomous: true
requirements: [IMMER-01, IMMER-02, IMMER-03]
files_modified:
  - app/routes/_index.jsx
  - app/components/ImmersiveTransition.jsx
commit_convention: "feat(05-02): description"
---

# Plan 05-02: Integrate immersive mode into _index.jsx

<objective>
Wire the immersive mode hook and transition component into `_index.jsx`. Wrap control panels with directional slide animations, render a centered floating timer pill during immersive mode, and ensure celebration overlay restores panel visibility on session end.
</objective>

<must_haves>
- Left column (timer card + vibe selector + scene selector) slides left 40px with fade
- Right column (mixer) slides right 40px with fade
- Hero text slides up 20px with fade
- Floating timer pill shows current time during immersive mode
- Panels restore when celebration overlay appears (session end)
- Relax page unaffected
</must_haves>

<tasks>

<task number="1" goal="Integrate immersive mode into _index.jsx">
<action>
Update `app/routes/_index.jsx` to:

1. Import `useImmersiveMode` and `ImmersiveTransition`
2. Call `useImmersiveMode(timer.isRunning)` after the timer hook
3. Wrap hero text in a transition div that fades+slides up when immersive
4. Wrap left column panels in a div that fades+slides left when immersive
5. Wrap right column (mixer) in a div that fades+slides right when immersive
6. Render the floating timer pill (centered, dark translucent) with current time during immersive mode
7. Ensure `showPanels` is forced true when `showCelebration` is active (celebration restores panels)

Animation mapping:
- Hero: `translateY: showPanels ? 0 : -20`
- Left column: `translateX: showPanels ? 0 : -40`
- Right column: `translateX: showPanels ? 0 : 40`

The floating timer pill renders the same `formatTime(timeLeft)` digits, styled larger (text-6xl), white text on dark translucent background.
</action>
<read_first>
- app/routes/_index.jsx (current structure)
- app/hooks/useImmersiveMode.js (created in 05-01)
- app/components/ImmersiveTransition.jsx (created in 05-01)
- 05-CONTEXT.md (D-01 through D-07)
- 05-UI-SPEC.md (pill typography, color, spacing)
</read_first>
<acceptance_criteria>
- `useImmersiveMode` imported and called in `_index.jsx`
- Hero text hidden during immersive mode with translateY(-20px)
- Left column panels slide left (-40px) with fade during immersive
- Right column panels slide right (40px) with fade during immersive
- Floating pill visible during immersive mode showing timer digits
- Panels restore when celebration overlay appears
- `npm run dev` starts without errors
</acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds (no JSX/import errors)
- [ ] Dev server starts without errors
- [ ] Starting a session hides panels, Escape reveals them
- [ ] Bottom edge hover reveals panels
- [ ] Timer end + celebration restores panels
- [ ] Relax page unaffected
</verification>
