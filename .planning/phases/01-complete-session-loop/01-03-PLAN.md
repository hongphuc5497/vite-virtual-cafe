---
phase: 01-complete-session-loop
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/routes/session.tsx
autonomous: true
requirements:
  - ROUTE-01
must_haves:
  truths:
    - "/session route no longer exists (returns 404 or does not resolve)"
    - "No build errors after removing the route"
    - "No imports or links reference the deleted route anywhere in the codebase"
  artifacts:
    - path: "app/routes/session.tsx"
      provides: "Deleted dead route file"
      min_lines: 0
  key_links:
    - from: "Removed file"
      to: "Remix file-based routing"
      via: "File deletion removes the route; no routing config change needed"
      pattern: "MISSING (file deleted)"
---

<objective>
Remove the dead /session route that only redirects to /.

Purpose: The /session route exists as a dead redirect-only file (ROUTE-01). It has no UI and no active references. Deleting it removes dead code and prevents confusion about available routes.

Output:
- app/routes/session.tsx -- deleted
</objective>

<execution_context>
@/Users/hongphuc/playground/vite-virtual-cafe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hongphuc/playground/vite-virtual-cafe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-complete-session-loop/01-CONTEXT.md (D-12)
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Delete the dead /session route and verify no references exist</name>
<files>
  app/routes/session.tsx
</files>

<read_first>
  app/routes/session.tsx
</read_first>

<action>
Step A: Verify the route file is dead

Read app/routes/session.tsx. It should contain only:
```typescript
import { redirect } from "@remix-run/node";

export async function loader() {
  return redirect("/");
}

export default function Session() {
  return null;
}
```

This is a dead route that immediately redirects to "/". The default export returns null because nothing renders. Confirmed dead by RESEARCH.md audit.

Step B: Verify no references exist before deleting

Run these grep commands to confirm the route is not referenced anywhere:
```bash
grep -rn "session" app/routes/ app/components/ app/hooks/ app/lib/ app/constants/ app/types/ --include="*.tsx" --include="*.ts" 2>/dev/null | grep -v "node_modules" | grep -v "SessionTimer" | grep -v "useSessionTimer" | grep -v "Session Complete" | grep -v "session length" | grep -v "Session Log" | head -20
```

This should show no references to the /session route file (excluding legitimate uses of "SessionTimer", "useSessionTimer", and similar).

Also check for route references:
```bash
grep -rn "/session" app/ --include="*.tsx" --include="*.ts" 2>/dev/null
```

This should return no results since root.tsx NAV_ITEMS only includes "/" and "/relax".

Step C: Delete the file

Delete app/routes/session.tsx.

Step D: Verify build still passes

Run `npm run typecheck` and `npm run build` to confirm deletion does not break anything.

Per D-12: "If the route file doesn't exist on disk, verify no imports or links reference it." Since the file exists and is being deleted, both the pre-deletion verification and post-deletion build are required.
</action>

<acceptance_criteria>
- app/routes/session.tsx is deleted (file does not exist)
- No grep results for "/session" route references in app/ source
- npm run typecheck passes with zero errors
- npm run build completes without errors
</acceptance_criteria>

<verify>
  <automated>test ! -f app/routes/session.tsx && echo "File deleted"</automated>
  <automated>npm run typecheck 2>&1 | tail -3</automated>
</verify>

<done>
Dead /session route file deleted. No references found in codebase. TypeScript check and build pass without errors. The route is no longer navigable.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries apply. Route deletion is a filesystem operation with no runtime impact.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | -- | route deletion | accept | Removing a dead route with zero references presents no attack surface. Verification steps confirm no build breaks. |
</threat_model>

<verification>
1. `test ! -f app/routes/session.tsx` confirms file deleted
2. `npm run typecheck` passes with zero errors
3. `npm run build` completes without errors
4. Manual: navigate to http://localhost:3000/session to confirm 404 or not found
</verification>

<success_criteria>
- [ ] app/routes/session.tsx deleted from disk per ROUTE-01 and D-12
- [ ] No build errors from file deletion
- [ ] No references to /session route exist in source code
- [ ] Removing a single redirect-only file requires no other code changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-complete-session-loop/01-03-SUMMARY.md`
</output>
