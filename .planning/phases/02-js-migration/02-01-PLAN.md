---
phase: 02-js-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/types/audio.ts (DELETE)
  - app/constants/audioConfig.ts -> .js
  - app/lib/utils.ts -> .js
  - app/lib/session.ts -> .js
  - app/hooks/useAudioManager.ts -> .js
  - app/hooks/useSessionTimer.ts -> .js
  - app/hooks/usePersistentState.ts -> .js
  - app/hooks/useKeyboardShortcuts.ts -> .js
  - app/components/BackdropOverlay.tsx -> .jsx
  - app/components/CelebrationOverlay.tsx -> .jsx
  - app/components/RoomMixControls.tsx -> .jsx
  - app/components/SessionTimer.tsx -> .jsx
  - app/components/TrackControl.tsx -> .jsx
  - app/components/VibeSelector.tsx -> .jsx
  - app/components/ui/button.tsx -> .jsx
  - app/routes/_index.tsx -> .jsx
  - app/routes/relax.tsx -> .jsx
  - app/entry.client.tsx -> .jsx
  - app/entry.server.tsx -> .jsx
  - app/root.tsx -> .jsx
  - vite.config.ts -> .js
  - tailwind.config.ts -> .js
autonomous: true
requirements:
  - MIGR-01
user_setup: []

must_haves:
  truths:
    - "All 22 TypeScript files are converted to .js/.jsx with types stripped and zero runtime logic changes"
    - "app/types/audio.ts is deleted (no runtime code, only type exports)"
    - "No .ts or .tsx files remain in app/ directory"
    - "No `import type`, `satisfies`, `as` casts, `declare module`, or type annotations exist in any converted file"
    - "vite.config.js uses `resolve.alias` for `~` path mapping (not `vite-tsconfig-paths`)"
    - "Entry.server.jsx has its TS-disable comment updated to `// eslint-disable-next-line no-unused-vars`"
  artifacts:
    - path: "app/constants/audioConfig.js"
      provides: "No `import type` line, no `as const` on DEFAULT_SCENE"
      contains: "export const DEFAULT_SCENE = \"misty-cabin\""
    - path: "app/lib/utils.js"
      provides: "Mixed import cleaned (only value imports remain)"
      contains: "import { clsx } from \"clsx\""
    - path: "app/lib/session.js"
      provides: "Interface removed, function params stripped of types"
      contains: "export function writeSessionEntry(entry) {"
    - path: "app/hooks/usePersistentState.js"
      provides: "Generic T removed from function and useState"
      contains: "export function usePersistentState("
    - path: "app/hooks/useAudioManager.js"
      provides: "Type annotations stripped from params and state"
      contains: "export function useAudioManager(tracks, initialPausedTracks) {"
    - path: "app/hooks/useSessionTimer.js"
      provides: "Stripped type annotation on param"
      contains: "export function useSessionTimer(initialDurationMinutes) {"
    - path: "app/hooks/useKeyboardShortcuts.js"
      provides: "Interface removed, callbacks param stripped"
      contains: "export function useKeyboardShortcuts(callbacks) {"
    - path: "app/components/BackdropOverlay.jsx"
      provides: "Interface removed, param type stripped"
      contains: "export function BackdropOverlay({ backdropGlow, scene = \"misty-cabin\" }) {"
    - path: "app/components/CelebrationOverlay.jsx"
      provides: "Interface removed, param type stripped"
      contains: "export function CelebrationOverlay({"
    - path: "app/components/RoomMixControls.jsx"
      provides: "Interface removed, param type stripped"
      contains: "export function RoomMixControls({"
    - path: "app/components/SessionTimer.jsx"
      provides: "`as const` removed from DURATION_PRESETS"
      contains: "const DURATION_PRESETS = [25, 45, 60, 90];"
    - path: "app/components/TrackControl.jsx"
      provides: "React.CSSProperties cast removed from style"
      contains: "style={{ \"--track-fill\": `${value}%` }"
    - path: "app/components/VibeSelector.jsx"
      provides: "`as const` removed from VIBES array"
      contains: "export const VIBES = ["
    - path: "app/components/ui/button.jsx"
      provides: "VariantProps type import removed, inline param type stripped"
      contains: "function Button({"
    - path: "app/routes/_index.jsx"
      provides: "All type imports removed, `satisfies` removed from JSON.stringify"
      contains: "satisfies" # MUST NOT contain satisfies
    - path: "app/routes/relax.jsx"
      provides: "Type imports removed, `as` casts removed"
      contains: "const parsed = JSON.parse(saved)"
    - path: "app/entry.client.jsx"
      provides: "Identical JS version (no TS syntax in original)"
      contains: "import { RemixBrowser } from \"@remix-run/react\""
    - path: "app/entry.server.jsx"
      provides: "Type annotations removed, TS-disable comment changed to no-unused-vars"
      contains: "// eslint-disable-next-line no-unused-vars"
    - path: "app/root.jsx"
      provides: "Type import and inline param type removed"
      contains: "export function Layout({ children }) {"
    - path: "vite.config.js"
      provides: "resolve.alias configured, tsconfigPaths removed, declare module block removed"
      contains: "resolve: {"
    - path: "tailwind.config.js"
      provides: "Type import removed, satisfies removed, content glob updated"
      contains: "content: [\"./app/**/{**,.client,.server}/**/*.{js,jsx}\"]"
  key_links:
    - from: "vite.config.js"
      to: "resolve.alias"
      via: "import path from \"node:path\" + import { fileURLToPath } from \"node:url\""
      pattern: "\"~\": path.resolve"
    - from: "All source files"
      to: "vite.config.js alias"
      via: "Import paths use ~/ prefix — resolved by Vite resolve.alias"
    - from: "All converted files"
      to: "No TS syntax remains"
      pattern: "grep -r -c 'satisfies\\|declare module\\|import type\\|: MixerTrack\\|: SceneId\\|: SessionEntry\\|: SavedPreferences\\|as const\\|React.CSSProperties\\|@typescript-eslint' app/ --include='*.{js,jsx}'"

---

<objective>
Convert all 22 TypeScript files (.ts/.tsx) to JavaScript (.js/.jsx) by stripping all type annotations, type imports, `as`/`satisfies` keywords, `declare module` blocks, and generic type parameters. Delete app/types/audio.ts (exports only types — empty after stripping). Configure vite.config.js with `resolve.alias` to replace `vite-tsconfig-paths`.

Purpose: This is the core conversion plan. Every .ts/.tsx file in the project gets mechanically converted to JS with zero runtime behavior changes. The file extension change triggers Remix/Vite to process the JS versions.
Output: 21 .js/.jsx files, 1 deleted file (types/audio.ts), 2 config files converted from .ts to .js
</objective>

<execution_context>
@/Users/hongphuc/playground/vite-virtual-cafe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hongphuc/playground/vite-virtual-cafe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/hongphuc/playground/vite-virtual-cafe/.planning/phases/02-js-migration/02-RESEARCH.md
@/Users/hongphuc/playground/vite-virtual-cafe/.planning/phases/02-js-migration/02-CONTEXT.md
@/Users/hongphuc/playground/vite-virtual-cafe/.planning/ROADMAP.md
@/Users/hongphuc/playground/vite-virtual-cafe/.planning/REQUIREMENTS.md

<interfaces>
Key types from the original TypeScript that all files depend on (must be removed, not converted):

From app/types/audio.ts:
```typescript
export type MixerTrack = { label: string; value: number; };
export type SceneId = "misty-cabin" | "sunday-morning" | "midnight-archive" | "rainy-metro";
export type SavedPreferences = {
  durationMinutes?: number; draftDurationMinutes?: number; appliedDurationMinutes?: number;
  tracks?: MixerTrack[]; pausedTracks?: Record<string, boolean>;
  soundEnabled?: boolean; selectedScene?: SceneId;
};
```

From app/lib/session.ts:
```typescript
export interface SessionEntry {
  date: string; durationMinutes: number; vibe: string; mood: string;
  tracksSnapshot: Record<string, number>;
}
```

These types are referenced via `import type` in 10 files. The import lines must be removed entirely (they have zero runtime effect).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert types layer, constants, lib, and all hooks</name>
  <files>
    app/types/audio.ts (DELETE)
    app/constants/audioConfig.ts -> .js
    app/lib/utils.ts -> .js
    app/lib/session.ts -> .js
    app/hooks/useAudioManager.ts -> .js
    app/hooks/useSessionTimer.ts -> .js
    app/hooks/usePersistentState.ts -> .js
    app/hooks/useKeyboardShortcuts.ts -> .js
  </files>
  <read_first>
    app/types/audio.ts
    app/constants/audioConfig.ts
    app/lib/utils.ts
    app/lib/session.ts
    app/hooks/useAudioManager.ts
    app/hooks/useSessionTimer.ts
    app/hooks/usePersistentState.ts
    app/hooks/useKeyboardShortcuts.ts
  </read_first>
  <action>
    Convert 8 files in the lowest dependency layer. Per D-01 and D-05, perform manual line-by-line type stripping with no JSDoc added. For each file:

    === app/types/audio.ts (DELETE) ===
    This file exports only type definitions (MixerTrack, SceneId, SavedPreferences). After type stripping it is empty. Delete the file entirely. All `import type` references to it are removed in other files below.

    === app/constants/audioConfig.ts -> .js ===
    1. Remove `import type { MixerTrack } from "~/types/audio";` entirely
    2. Change `export const DEFAULT_SCENE = "misty-cabin" as const;` to `export const DEFAULT_SCENE = "misty-cabin";`
    3. Change `export const DEFAULT_TRACKS: MixerTrack[] = [` to `export const DEFAULT_TRACKS = [`
    4. Keep all data values identical (track labels, volumes, URLs)

    === app/lib/utils.ts -> .js ===
    1. Change `import { clsx, type ClassValue } from "clsx"` to `import { clsx } from "clsx"` (remove `type ClassValue`)
    2. Change `export function cn(...inputs: ClassValue[]) {` to `export function cn(...inputs) {`

    === app/lib/session.ts -> .js ===
    1. Remove `import type { MixerTrack } from "~/types/audio";` entirely
    2. Remove the `export interface SessionEntry { ... }` block entirely (lines 18-24)
    3. Change `export function detectVibeName(tracks: MixerTrack[]): string {` to `export function detectVibeName(tracks) {`
    4. Change `const track = tracks.find((t) => t.label === label);` — `find` callback already has no type annotation, keep as is
    5. Change `export function detectMood(tracks: MixerTrack[], vibeName: string): string {` to `export function detectMood(tracks, vibeName) {`
    6. Change line `const barista = tracks.find((t) => t.label === "Barista")?.value ?? 0;` — no TS syntax, keep as is
    7. Change `export function writeSessionEntry(entry: SessionEntry): void {` to `export function writeSessionEntry(entry) {`
    8. Change `const sessions: SessionEntry[] = existing ? JSON.parse(existing) : [];` to `const sessions = existing ? JSON.parse(existing) : [];`
    9. Keep all runtime logic, comments, and string values identical

    === app/hooks/useAudioManager.ts -> .js ===
    1. Remove `import type { MixerTrack } from "~/types/audio";` entirely
    2. Change `export function useAudioManager(tracks: MixerTrack[], initialPausedTracks?: Record<string, boolean>) {` to `export function useAudioManager(tracks, initialPausedTracks) {`
    3. Change `useState<Record<string, boolean>>(` to `useState(`
    4. Change `useRef<Map<string, HTMLAudioElement>>(new Map())` to `useRef(new Map())`
    5. Keep all runtime logic, string values, volume curve exponent identical

    === app/hooks/useSessionTimer.ts -> .js ===
    1. Change `export function useSessionTimer(initialDurationMinutes: number) {` to `export function useSessionTimer(initialDurationMinutes) {`
    2. Change `useRef<number | null>(null)` to `useRef(null)`
    3. Keep all runtime logic identical

    === app/hooks/usePersistentState.ts -> .js ===
    1. Change `export function usePersistentState<T>(` to `export function usePersistentState(`
    2. Change `key: string, initialState: T, serialize?: (state: T) => string, deserialize?: (json: string) => T` to `key, initialState, serialize, deserialize`
    3. Change `useState<T>(initialState)` to `useState(initialState)`
    4. Keep all runtime logic identical

    === app/hooks/useKeyboardShortcuts.ts -> .js ===
    1. Remove the inline interface type for the callbacks parameter
    2. Change `export function useKeyboardShortcuts(callbacks: { onTogglePlay?: () => void; onVolumeIncrease?: () => void; onVolumeDecrease?: () => void; }) {` to `export function useKeyboardShortcuts(callbacks) {`

    For every conversion: rename the source file to its .js/.jsx extension (delete the .ts/.tsx original), then write the cleaned JS content.
  </action>
  <acceptance_criteria>
    - [ ] app/types/audio.ts no longer exists on disk: `test ! -f app/types/audio.ts`
    - [ ] app/constants/audioConfig.js exists with `export const DEFAULT_SCENE = "misty-cabin"` (no `as const`)
    - [ ] app/lib/utils.js exists with `import { clsx } from "clsx"` (no `type ClassValue`)
    - [ ] app/lib/session.js exists with `export function writeSessionEntry(entry) {` (no `: SessionEntry`)
    - [ ] app/hooks/useAudioManager.js exists with `export function useAudioManager(tracks, initialPausedTracks) {`
    - [ ] app/hooks/usePersistentState.js exists with `export function usePersistentState(` (no `<T>`)
    - [ ] app/hooks/useKeyboardShortcuts.js exists with `export function useKeyboardShortcuts(callbacks) {`
    - [ ] No files in app/types/, app/constants/, app/lib/, app/hooks/ have `.ts` extension: `ls app/types/constants/lib/hooks/*.{ts,tsx} 2>&1` should all return "No such file"
    - [ ] All converted files are valid JS: `node -e "require('fs').readdirSync('app/lib').filter(f=>f.endsWith('.js')).forEach(f=>new Function(require('fs').readFileSync('app/lib/'+f,'utf8'))); console.log('ok')"`
  </acceptance_criteria>
  <verify>
    <automated>for f in app/constants/audioConfig.js app/lib/utils.js app/lib/session.js app/hooks/useAudioManager.js app/hooks/useSessionTimer.js app/hooks/usePersistentState.js app/hooks/useKeyboardShortcuts.js; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done; test ! -f app/types/audio.ts && echo "types/audio.ts deleted" && echo "All files OK"</automated>
  </verify>
  <done>
    8 files converted/created. app/types/audio.ts deleted. All type annotations and type imports removed. Runtime logic identical to original.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert all components (.tsx -> .jsx)</name>
  <files>
    app/components/BackdropOverlay.tsx -> .jsx
    app/components/CelebrationOverlay.tsx -> .jsx
    app/components/RoomMixControls.tsx -> .jsx
    app/components/SessionTimer.tsx -> .jsx
    app/components/TrackControl.tsx -> .jsx
    app/components/VibeSelector.tsx -> .jsx
    app/components/ui/button.tsx -> .jsx
  </files>
  <read_first>
    app/components/BackdropOverlay.tsx
    app/components/CelebrationOverlay.tsx
    app/components/RoomMixControls.tsx
    app/components/SessionTimer.tsx
    app/components/TrackControl.tsx
    app/components/VibeSelector.tsx
    app/components/ui/button.tsx
  </read_first>
  <action>
    Convert all 7 component files from .tsx to .jsx. Per D-05, strip all type annotations completely with no JSDoc.

    === app/components/BackdropOverlay.tsx -> .jsx ===
    1. Remove `import type { SceneId } from "~/types/audio";` entirely
    2. Change `const SCENE_TINTS: Record&lt;SceneId, string&gt; = {` to `const SCENE_TINTS = {`
    3. Remove the `export interface BackdropOverlayProps { ... }` block entirely
    4. Change `export function BackdropOverlay({ backdropGlow, scene = "misty-cabin" }: BackdropOverlayProps) {` to `export function BackdropOverlay({ backdropGlow, scene = "misty-cabin" }) {`
    5. Keep all values and logic identical

    === app/components/CelebrationOverlay.tsx -> .jsx ===
    1. Remove `import type { SessionEntry } from "~/lib/session";` entirely
    2. Remove the `export interface CelebrationOverlayProps { ... }` block entirely
    3. Change `export function CelebrationOverlay({ session, show, onDismiss }: CelebrationOverlayProps) {` to `export function CelebrationOverlay({ session, show, onDismiss }) {`
    4. Keep all JSX, styles, text content identical

    === app/components/RoomMixControls.tsx -> .jsx ===
    1. Remove `import type { MixerTrack } from "~/types/audio";` entirely
    2. Remove the `export interface RoomMixControlsProps { ... }` block entirely
    3. Change `export function RoomMixControls({ ... }: RoomMixControlsProps) {` to `export function RoomMixControls({ soundEnabled, tracks, pausedTracks, trackErrors, onToggleSound, onTrackVolumeChange, onTrackPauseToggle, onTrackRetry }) {`
    4. Keep all JSX and logic identical

    === app/components/SessionTimer.tsx -> .jsx ===
    1. Change `const DURATION_PRESETS = [25, 45, 60, 90] as const;` to `const DURATION_PRESETS = [25, 45, 60, 90];`
    2. Remove the `export interface SessionTimerProps { ... }` block entirely
    3. Change `export function SessionTimer({ ... }: SessionTimerProps) {` to `export function SessionTimer({ timeLeft, totalSeconds, isRunning, draftDurationMinutes, onStart, onReset, onDurationChange, formatTime }) {`
    4. Keep all JSX, SVG math, and logic identical

    === app/components/TrackControl.tsx -> .jsx ===
    1. Remove `import type React from "react";` entirely
    2. Remove the `export interface TrackControlProps { ... }` block entirely
    3. Change `export function TrackControl({ ... }: TrackControlProps) {` to `export function TrackControl({ label, value, isPaused, hasError, onVolumeChange, onTogglePause, onRetry }) {`
    4. Change `style={{ "--track-fill": `${value}%` } as React.CSSProperties}` to `style={{ "--track-fill": `${value}%` }}` — remove the `as React.CSSProperties` cast
    5. Keep all JSX, strings, and logic identical

    === app/components/VibeSelector.tsx -> .jsx ===
    1. Remove `import type { MixerTrack } from "~/types/audio";` entirely
    2. Change `export const VIBES = [ ... ] as const;` to `export const VIBES = [ ... ];` — remove `as const` suffix
    3. Remove the `export interface VibeSelectorProps { ... }` block entirely
    4. Change `export function VibeSelector({ tracks, onApplyVibe }: VibeSelectorProps) {` to `export function VibeSelector({ tracks, onApplyVibe }) {`
    5. Keep all data values, JSX, and logic identical

    === app/components/ui/button.tsx -> .jsx ===
    1. Change `import { cva, type VariantProps } from "class-variance-authority"` to `import { cva } from "class-variance-authority"` (remove `type VariantProps`)
    2. Change `function Button({ className, variant = "default", size = "default", ...props }: ButtonPrimitive.Props & VariantProps&lt;typeof buttonVariants&gt;) {` to `function Button({ className, variant = "default", size = "default", ...props }) {`
    3. Keep all cva config, className logic, and JSX identical

    For every conversion: rename the source file to its .jsx extension (delete the .tsx original), then write the cleaned JSX content.
  </action>
  <acceptance_criteria>
    - [ ] All 7 .jsx files exist: `ls app/components/*.jsx app/components/ui/*.jsx` returns 7 files
    - [ ] No .tsx files remain in app/components/: `ls app/components/*.tsx app/components/ui/*.tsx 2>&1` returns "No such file"
    - [ ] BackdropOverlay.jsx contains `export function BackdropOverlay({ backdropGlow, scene = "misty-cabin" }) {` (no `: BackdropOverlayProps`)
    - [ ] SessionTimer.jsx contains `const DURATION_PRESETS = [25, 45, 60, 90];` (no `as const`)
    - [ ] TrackControl.jsx contains `style={{ "--track-fill": `${value}%` }}` (no `as React.CSSProperties`)
    - [ ] VibeSelector.jsx contains `export const VIBES = [` (no `as const`)
    - [ ] button.jsx contains `function Button({ className, variant = "default", size = "default", ...props }) {` (no `VariantProps<`)
    - [ ] No file contains `import type` or `export interface` in app/components/
  </acceptance_criteria>
  <verify>
    <automated>for f in app/components/BackdropOverlay.jsx app/components/CelebrationOverlay.jsx app/components/RoomMixControls.jsx app/components/SessionTimer.jsx app/components/TrackControl.jsx app/components/VibeSelector.jsx app/components/ui/button.jsx; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done; echo "All 7 component files OK"</automated>
  </verify>
  <done>
    7 component files converted from .tsx to .jsx. All interfaces removed. All `as const` suffixes, `React.CSSProperties` casts, and type imports stripped.
  </done>
</task>

<task type="auto">
  <name>Task 3: Convert routes, entry points, root layout, and config files — set up vite.config.js resolve.alias</name>
  <files>
    app/routes/_index.tsx -> .jsx
    app/routes/relax.tsx -> .jsx
    app/entry.client.tsx -> .jsx
    app/entry.server.tsx -> .jsx
    app/root.tsx -> .jsx
    vite.config.ts -> .js
    tailwind.config.ts -> .js
  </files>
  <read_first>
    app/routes/_index.tsx
    app/routes/relax.tsx
    app/entry.client.tsx
    app/entry.server.tsx
    app/root.tsx
    vite.config.ts
    tailwind.config.ts
  </read_first>
  <action>
    Convert all 7 remaining files. Per D-04, configure `resolve.alias` in vite.config.js instead of `vite-tsconfig-paths`. Per D-05, strip all type annotations completely with no JSDoc.

    === app/routes/_index.tsx -> .jsx ===
    1. Remove `import type { SessionEntry } from "~/lib/session";` entirely
    2. Remove `import type { MixerTrack, SavedPreferences, SceneId } from "~/types/audio";` entirely
    3. Change `useState&lt;SceneId&gt;(DEFAULT_SCENE)` to `useState(DEFAULT_SCENE)`
    4. Change `useState&lt;SessionEntry | null&gt;(null)` to `useState(null)`
    5. Change `const parsed = JSON.parse(saved) as SavedPreferences;` to `const parsed = JSON.parse(saved);`
    6. Change `JSON.stringify({ ... } satisfies SavedPreferences)` (line 91-97) to `JSON.stringify({ draftDurationMinutes, appliedDurationMinutes, tracks, pausedTracks: audio.pausedTracks, selectedScene })` — remove `satisfies SavedPreferences`
    7. Change the `onTrackRetry={(label: string) =>` to `onTrackRetry={(label) =>`
    8. Change `handleApplyVibe = (preset: Record<string, number>)` to `handleApplyVibe = (preset) =>`
    9. Change `setTracks((current: MixerTrack[]) =>` to `setTracks((current) =>`
    10. Keep all runtime logic, JSX, useEffect contents, and state setters identical

    === app/routes/relax.tsx -> .jsx ===
    1. Remove `import type { MixerTrack, SavedPreferences, SceneId } from "~/types/audio";` entirely
    2. Change `useState&lt;SceneId&gt;(DEFAULT_SCENE)` to `useState(DEFAULT_SCENE)`
    3. Change `const parsed = JSON.parse(saved) as SavedPreferences;` (both occurrences at lines 15 and 31) to `const parsed = JSON.parse(saved);`
    4. Change `setTracks((current: MixerTrack[]) =>` to `setTracks((current) =>`
    5. Keep all runtime logic, JSX, and localStorage access identical

    === app/entry.client.tsx -> .jsx ===
    This file has no TypeScript-specific syntax (no types, no annotations, no generic params). Simply rename from `.tsx` to `.jsx` with NO content changes.

    === app/entry.server.tsx -> .jsx ===
    1. Remove `import type { AppLoadContext, EntryContext } from "@remix-run/node";` entirely
    2. Change `request: Request, responseStatusCode: number, responseHeaders: Headers, remixContext: EntryContext,` to `request, responseStatusCode, responseHeaders, remixContext,`
    3. Change `// eslint-disable-next-line @typescript-eslint/no-unused-vars` to `// eslint-disable-next-line no-unused-vars`
    4. Change `loadContext: AppLoadContext` to `_loadContext` (rename to underscore-prefixed so `no-unused-vars` ignores it)
    5. Change `onShellError(error: unknown)` to `onShellError(error)` and `onError(error: unknown)` to `onError(error)`
    6. Keep all runtime logic, JSX, and string values identical

    === app/root.tsx -> .jsx ===
    1. Remove `import type { LinksFunction } from "@remix-run/node";` entirely
    2. Change `export const links: LinksFunction = () => [` to `export const links = () => [`
    3. Change `const NAV_ITEMS = [ ... ] as const;` to `const NAV_ITEMS = [ ... ];`
    4. Change `export function Layout({ children }: { children: React.ReactNode }) {` to `export function Layout({ children }) {`
    5. Keep all JSX, nav items, font links, and styling identical

    === vite.config.ts -> .js (entire rewrite with resolve.alias) ===
    Replace the entire file. Remove `import tsconfigPaths from "vite-tsconfig-paths";` and the `declare module` block. Add `resolve.alias` configuration.

    New file content:
    ```javascript
    import { vitePlugin as remix } from "@remix-run/dev";
    import { defineConfig } from "vite";
    import path from "node:path";
    import { fileURLToPath } from "node:url";

    const __dirname = path.dirname(fileURLToPath(import.meta.url));

    export default defineConfig({
      resolve: {
        alias: {
          "~": path.resolve(__dirname, "app"),
        },
      },
      plugins: [
        remix({
          future: {
            v3_fetcherPersist: true,
            v3_relativeSplatPath: true,
            v3_throwAbortReason: true,
            v3_singleFetch: true,
            v3_lazyRouteDiscovery: true,
          },
        }),
      ],
    });
    ```

    === tailwind.config.ts -> .js ===
    1. Remove `import type { Config } from "tailwindcss";` entirely
    2. Optionally add `/** @type {import('tailwindcss').Config} */` JSDoc comment at top (for IDE support, per Claude's discretion)
    3. Change `satisfies Config` (last line) to nothing — remove `satisfies Config;` and add `;` after the closing `}`
    4. Change content glob from `./app/**/{**,.client,.server}/**/*.{js,jsx,ts,tsx}` to `'./app/**/{**,.client,.server}/**/*.{js,jsx}'`
    5. Keep all color values, font families, and border radii identical

    The final tailwind.config.js should look like:
    ```javascript
    /** @type {import('tailwindcss').Config} */
    export default {
      content: ["./app/**/{**,.client,.server}/**/*.{js,jsx}"],
      theme: { extend: { /* ... all colors, fonts, radii identical to original ... */ } },
      plugins: [],
    };
    ```
  </action>
  <acceptance_criteria>
    - [ ] app/routes/_index.jsx exists with `useState(DEFAULT_SCENE)` (no `<SceneId>`), `const parsed = JSON.parse(saved)` (no `as SavedPreferences`), no `satisfies` keyword
    - [ ] app/routes/relax.jsx exists with `useState(DEFAULT_SCENE)`, `const parsed = JSON.parse(saved)` (no `as SavedPreferences`)
    - [ ] app/entry.server.jsx exists with `// eslint-disable-next-line no-unused-vars` and `_loadContext` parameter
    - [ ] app/root.jsx exists with `export function Layout({ children }) {` (no `: { children: React.ReactNode }`)
    - [ ] vite.config.js exists with `resolve: { alias: { "~": path.resolve(__dirname, "app") } }` (no `tsconfigPaths()`)
    - [ ] tailwind.config.js exists with content glob `./app/**/{**,.client,.server}/**/*.{js,jsx}` (no `ts,tsx`) and no `satisfies`
    - [ ] No .ts/.tsx files remain in app/routes/, app/ root: `ls app/routes/*.tsx app/*.tsx 2>&1` returns "No such file"
    - [ ] vite.config.ts no longer exists, tailwind.config.ts no longer exists
  </acceptance_criteria>
  <verify>
    <automated>for f in app/routes/_index.jsx app/routes/relax.jsx app/entry.client.jsx app/entry.server.jsx app/root.jsx vite.config.js tailwind.config.js; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done; echo "All 7 files OK"</automated>
  </verify>
  <done>
    7 files converted. vite.config.js has resolve.alias configured per D-04. All type imports, satisfies keywords, declare module blocks, and type annotations removed. ESLint comment updated in entry.server.jsx.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build toolchain -> filesystem | Vite config reads source files, writes build output |
| ESLint -> source files | Linter processes all JS/JSX files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Source file conversion | mitigate | Each file is read, stripped of types, and written as JS. No regex find-replace or automated tool — manual line-by-line ensures no logic is dropped. |
| T-02-02 | Tampering | `satisfies` keyword remaining in JS | mitigate | The executor MUST grep for `satisfies` in every converted file. There are exactly 2 instances in the codebase (_index.tsx line 97, tailwind.config.ts line 91). If either remains in a .js/.jsx file, it causes a SyntaxError. |
| T-02-03 | Tampering | `declare module` block in vite.config.js | mitigate | The `declare module` block is removed from vite.config.ts during conversion. It had zero runtime effect (only TS compile-time). Confirmed removed by verifying no `declare module` string in vite.config.js. |
| T-02-04 | Information Disclosure | ESLint disable comment | accept | The `@typescript-eslint/no-unused-vars` comment is changed to `no-unused-vars` in entry.server.jsx. No information disclosure risk since this is a local app with no PII filtering through the server entry. |
</threat_model>

<verification>
- All 22 .ts/.tsx files are replaced by .js/.jsx equivalents
- app/types/audio.ts is deleted
- No `.ts` or `.tsx` extension files remain in `app/`, `vite.config.ts`, or `tailwind.config.ts`
- All converted files have zero type annotations, type imports, `satisfies`, `as` casts, `declare module`, or generic type parameters
- vite.config.js has `resolve.alias` configured for `~` path mapping (no `vite-tsconfig-paths` plugin)
- The `satisfies` keyword does not appear in any `.js`/`.jsx` file
- The `as const` pattern does not appear in any `.js`/`.jsx` file
- entry.server.jsx has `_loadContext` (underscore-prefixed) and `// eslint-disable-next-line no-unused-vars`
</verification>

<success_criteria>
1. All 22 TypeScript source files are converted to JavaScript (22 .js/.jsx files created from 22 .ts/.tsx originals)
2. app/types/audio.ts is deleted (contained only type exports, no runtime code)
3. vite.config.js uses `resolve.alias` for `~` path mapping, no `vite-tsconfig-paths` plugin
4. tailwind.config.js has content glob updated to `*.{js,jsx}` (no `ts,tsx`)
5. entry.server.jsx has updated ESLint disable comment (`no-unused-vars` instead of `@typescript-eslint/no-unused-vars`) and `_loadContext` parameter
6. No `.ts` or `.tsx` files remain in app/ directory, vite.config.ts, or tailwind.config.ts
7. Every converted file has zero type annotations, type imports, `as`/`satisfies`/`declare module`/generic syntax — verified by grep
</success_criteria>

<output>
After completion, create .planning/phases/02-js-migration/02-01-SUMMARY.md
</output>
