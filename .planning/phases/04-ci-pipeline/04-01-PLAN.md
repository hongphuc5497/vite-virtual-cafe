---
phase: 4
phase_name: CI Pipeline
plan: 04-01
plan_name: Create GitHub Actions CI workflow
wave: 1
depends_on: []
autonomous: true
requirements: [CI-01]
files_modified:
  - .github/workflows/playwright.yml
  - playwright.config.js
commit_convention: "ci(04-01): description"
---

# Plan 04-01: Create GitHub Actions CI workflow

<objective>
Create `.github/workflows/playwright.yml` that runs the Playwright E2E test suite on every push and pull request to `main`. The workflow installs dependencies, builds the app, runs tests across Chromium/Firefox/WebKit in parallel, and reports status as a PR check that blocks merge on failure.
</objective>

<must_haves>
- Workflow triggers on push and pull_request to main
- Multi-browser matrix: Chromium, Firefox, WebKit
- Playwright browsers installed with dependencies
- Test results uploaded as artifacts on failure
- PR status check blocks merge on failure
</must_haves>

<tasks>

<task number="1" goal="Create GitHub Actions workflow file">
<action>
Create `.github/workflows/playwright.yml`:

```yaml
name: Playwright E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Playwright (${{ matrix.browser }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - run: npm run build

      - name: Install Playwright browsers
        run: npx playwright install --with-deps ${{ matrix.browser }}

      - name: Run Playwright tests
        run: npx playwright test --project=${{ matrix.browser }}

      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report-${{ matrix.browser }}
          path: |
            playwright-report/
            test-results/
          retention-days: 7
```

Update `playwright.config.js` to add Firefox and WebKit projects alongside Chromium.
</action>
<read_first>
- playwright.config.js
- package.json (check Node version, scripts)
</read_first>
<acceptance_criteria>
- `.github/workflows/playwright.yml` exists
- YAML has `on: push: branches: [main]` and `pull_request: branches: [main]`
- Matrix includes chromium, firefox, webkit
- `npx playwright install --with-deps` step present
- Artifact upload on failure configured
- `playwright.config.js` has projects for all 3 browsers
</acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `.github/workflows/playwright.yml` is valid YAML
- [ ] Workflow triggers match push + PR to main
- [ ] Matrix strategy covers all 3 browsers
- [ ] `playwright.config.js` has Chromium, Firefox, WebKit projects
</verification>
